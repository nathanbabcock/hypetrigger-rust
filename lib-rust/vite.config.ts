import { defineConfig } from 'vite'

export default defineConfig({
  // plugins: [solidPlugin()],
  // build: {
  //   target: 'esnext',
  //   polyfillDynamicImport: false,
  // },
  server: {
    open: true,
  }
})
