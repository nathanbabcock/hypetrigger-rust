{
  "compilerOptions": {
    "baseUrl": "src", // This must be specified if "paths" is.
    "paths": {
      // Not used because Typescript has trouble following a chain of paths.
      "hypetrigger-wasm": ["../../lib-rust/pkg/hypetrigger"]
    },
    "outDir": "dist"
  }
}
